---
version: 2
jobs:
  build:
    docker:
      - image: crystallang/crystal:latest

    steps:
      - checkout
      - run:
          name: Install Shards
          command: |
            shards install

      - run:
          name: Run Tests
          command: |
            crystal spec
